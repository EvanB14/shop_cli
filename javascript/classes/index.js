const { User, Users } = require('./Users');
const { Product, Products  } = require('./Products');
const Cart = require('./Cart');

module.exports = {
    Product,
    Products,
    User,
    Users,
    Cart
}